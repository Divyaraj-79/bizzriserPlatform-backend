const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const orgs = await prisma.organization.findMany({ include: { package: true } }); for (const org of orgs) { if (org.package && org.package.credits !== undefined && org.credits === 0) { await prisma.organization.update({ where: { id: org.id }, data: { credits: org.package.credits } }); console.log('Synced org', org.id, 'to', org.package.credits); } } } main().catch(console.error).finally(() => prisma.$disconnect());
