const { PrismaClient } = require('@prisma/client');\nconst prisma = new PrismaClient();\n\nasync function main() {\n  console.log('Fetching details from DB...');\n  const account = await prisma.whatsAppAccount.findFirst();\n  const user = await prisma.user.findFirst();\n  \n  console.log('ACCOUNT ID:', account ? account.id : 'none');\n  console.log('PHONE NUMBER ID:', account ? account.phoneNumberId : 'none');\n  console.log('ORG ID:', user ? user.organizationId : 'none');\n  console.log('USER ID:', user ? user.id : 'none');\n  console.log('USER EMAIL:', user ? user.email : 'none');\n}\n\nmain().finally(() => prisma.$disconnect());\n