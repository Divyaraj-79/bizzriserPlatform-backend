const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const superadmins = await prisma.user.findMany({ where: { role: 'SUPER_ADMIN' }, select: { organizationId: true } }); for (const admin of superadmins) { if (admin.organizationId) { await prisma.organization.update({ where: { id: admin.organizationId }, data: { credits: -1 } }); console.log('Updated org', admin.organizationId); } } } main().catch(console.error).finally(() => prisma.$disconnect());
