const fs = require('fs');\nconst path = require('path');\n\n// Simulate what the frontend does: read the existing WebP file stored as .jpg\nconst uploadDir = path.join(process.cwd(), 'uploads');\nconst files = fs.readdirSync(uploadDir).filter(f => f.endsWith('.jpg'));\nif (files.length === 0) { console.log('No .jpg files found'); process.exit(0); }\n\nconst filename = files[0];\nconst buf = fs.readFileSync(path.join(uploadDir, filename));\nconsole.log(`File: ${filename}, size: ${buf.length}`);\nconsole.log(`First 12 bytes: ${buf.slice(0, 12).toString('hex')}`);\n\n// Detect WebP\nconst isWebP = buf[0] === 0x52 && buf[1] === 0x49 && buf[2] === 0x46 && buf[3] === 0x46 &&\n               buf[8] === 0x57 && buf[9] === 0x45 && buf[10] === 0x42 && buf[11] === 0x50;\nconst isJPEG = buf[0] === 0xFF && buf[1] === 0xD8;\nconst isPNG  = buf[0] === 0x89 && buf[1] === 0x50;\n\nconsole.log(`isWebP: ${isWebP}, isJPEG: ${isJPEG}, isPNG: ${isPNG}`);\n\n// Simulate fetch + FormData send to messaging endpoint\nconst { fileTypeFromBuffer } = require('file-type');\nfileTypeFromBuffer(buf).then(detected => {\n  console.log(`Detected MIME: ${detected?.mime}, ext: ${detected?.ext}`);\n}).catch(e => console.error('Error:', e));\n