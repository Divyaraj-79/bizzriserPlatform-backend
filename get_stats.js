const fs = require('fs');\nconst path = require('path');\n\nconst filePath = path.join(__dirname, 'dist', 'src', 'modules', 'campaigns', 'campaign.processor.js');\ntry {\n  const stats = fs.statSync(filePath);\n  console.log('File stats for campaign.processor.js:');\n  console.log('Size:', stats.size);\n  console.log('Modified time:', stats.mtime);\n  console.log('Birth time:', stats.birthtime);\n} catch (error) {\n  console.error('Error reading stats:', error.message);\n}\n